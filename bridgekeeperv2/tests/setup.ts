/**
 * Test setup and shared utilities for vitest.
 */

import { beforeAll, afterAll } from 'vitest';

// Global test timeout
beforeAll(() => {
    // Increase timeout for async tests
});

afterAll(() => {
    // Cleanup
});
